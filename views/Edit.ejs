<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit User</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <div class="max-w-lg mx-auto mt-10 p-6 bg-white rounded-lg shadow-md">
    <h2 class="text-2xl font-bold mb-4">Edit User</h2>
    <form action="/update-note/<%= user._id %>" method="get">
      <div class="mb-4">
        <label class="block text-gray-700 font-semibold mb-2" for="name">Name</label>
        <input class="w-full px-3 py-2 border rounded-lg" type="text" id="name" name="name" value="<%= user.name %>" required>
      </div>
      <div class="mb-4">
        <label class="block text-gray-700 font-semibold mb-2" for="age">Age</label>
        <input class="w-full px-3 py-2 border rounded-lg" type="number" id="age" name="age" value="<%= user.age %>" required>
      </div>
      <div class="mb-4">
        <label class="block text-gray-700 font-semibold mb-2" for="email">Email</label>
        <input class="w-full px-3 py-2 border rounded-lg" type="email" id="email" name="email" value="<%= user.email %>" required>
      </div>
      <button class="w-full bg-blue-500 text-white py-2 rounded-lg hover:bg-blue-600" type="submit">Update User</button>
    </form>
  </div>
</body>
</html>
